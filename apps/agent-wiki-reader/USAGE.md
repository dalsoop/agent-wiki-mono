# AgentWikiReader 사용법

메뉴바 아이콘 → 상태 확인 / 새로고침 / 설정.
설정창에서 언어(System/한국어/English)를 즉시 전환할 수 있다.

CLI 바이너리 이름: `agent-wiki-reader` (executable product `AgentWikiReader`).

의존성: `agent-wiki-reader capabilities` → `.result.depends` (목록은 여기 두지 않는다.
정본: `docs/app-interop-contract.md`).
