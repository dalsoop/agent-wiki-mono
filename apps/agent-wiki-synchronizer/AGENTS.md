# agent-wiki-global

## 목표

<!-- 이 앱이 무엇을 하는지 한두 문단. 에이전트가 이 파일만 읽고 앱을 다룰 수 있어야 한다. -->

## 지금 범위

<!-- 구현된 것과 **없는 것**을 같이 적는다. 없는 걸 안 적으면 에이전트가 있다고 가정한다. -->

- 저장 경로:
- GUI:
- CLI:
- **없음**:

## CLI

이 앱의 도메인 조작은 CLI가 문이다(App CLI First). 상태 파일 직접 수정·GUI 클릭 우회 금지.

```bash
agent-wiki-global capabilities   # 계약 출력 — 먼저 이걸 본다
agent-wiki-global --help
```

## 상태

- StateMirror: `~/.swift-app-state/agent-wiki-global.json`
- 헬스 펄스: `~/.swift-app-state/pulse/agent-wiki-global.pulse`

## 다음 작업 후보

1.
2.

## 함정

<!-- 실측으로 확인된 것만. 추측은 적지 않는다. -->
